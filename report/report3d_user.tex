\documentclass[11pt]{article}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath, amssymb, amsthm}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{setspace}
\usepackage{biblatex}
\usepackage{arydshln}
\usepackage{subcaption}
\usepackage{mathtools}
\setstretch{1.15}
\addbibresource{references.bib} %Import the bibliography file

\title{\textbf{Quasi-static model of the chinese paper yo-yo}}
\author{Brandon Wong, Ben Foster}
\date{\today}

\begin{document}

\maketitle


\section{Geometric model}\label{sec:setup}
\begin{figure}
    \centering
    \includegraphics[width=0.5\textwidth]{figs/capstan_helix.pdf}
    \caption{in progress}\label{fig:setup}
\end{figure}

The system can be modelled as a constant-width rectangular ribbon of negligible thickness wrapped around a uniform-radius cylinder at a uniform pitch. One end of the ribbon is fixed in place to prevent rigid body motion, while the other (``active'' end) is displaced in the $z$ direction (the axis of the cylinder). Refer to Figure~\ref{fig:setup} for a visual representation of the system. The system has the following geometric constants:
\begin{itemize}
    \item $L$: the length of the unrolled rectangle.
    \item $W$: the width of the unrolled rectangle. Assume $L \gg W$.
    \item $\theta_L$: the total wind angle of the helix, which is fixed based on the boundary conditions (the two ends of the ribbon cannot rotate about the $z$-axis relative to one another, only translate along it, so the total number of turns is a fixed topological property of how the ribbon was wound and does not change as it is pulled). $\theta_L = 2\pi n$ where $n$ is the number of winds around the $z$ axis (not necessarily integer).
\end{itemize}

The state of the system is parametrized by the parameter $x$, which describes how far the active end has displaced in the $z$ direction relative to the base end.

\subsection{Coordinate systems}
Positions on the helix can be described with local surface $u,v$ coordinates, which describe the unrolled rectangle. The $u$ direction runs along the length of the unrolled rectangle, and the $v$ direction runs across its width. Alternatively, positions can be described in 3d space using cylindrical $r,\theta,z$ coordinates.

To simplify the geometry, we assume that the helix has a constant radius $r$ and a constant pitch angle $\phi$ (the angle the local $\hat{u}$ makes with the $r,\theta$ plane). Equivalently, the local orthonormal frame $(\hat u,\hat v)$ tangent to the ribbon surface is related to the cylinder's own natural frame $(\hat\theta,\hat z)$ by a rotation through the angle $\phi$:
\begin{equation}\label{eq:frame_rotation}
    \hat u = \cos\phi\,\hat\theta + \sin\phi\,\hat z, \qquad
    \hat v = -\sin\phi\,\hat\theta + \cos\phi\,\hat z.
\end{equation}
% ($\hat v$ is obtained by rotating $\hat u$ a further $90^\circ$ in the same sense, so that $(\hat u,\hat v)$ remains a right-handed orthonormal frame like $(\hat\theta,\hat z)$.) Equation~\eqref{eq:frame_rotation} is the single geometric fact that determines everything else about the shape of the ribbon: because $\phi$ is just the angle between the ribbon's own axes and the cylinder's axes, every quantity that depends on this rotation will naturally involve $\cos\phi,\sin\phi$, and---once such quantities are combined pairwise, as stresses and curvatures are---their double-angle combinations $\cos^2\phi,\sin^2\phi,\sin2\phi$. This same pattern will reappear repeatedly below.

We now use Eq.~\eqref{eq:frame_rotation} to relate the geometric constants $L,\theta_L$ to the pitch angle $\phi$ and radius $r$ at a given extension $x$. From Eq.~\eqref{eq:frame_rotation}, $\hat u$ has $z$-component $\sin\phi$, so moving a distance $\mathrm du$ along the ribbon's own length produces an axial displacement $\mathrm dz=\sin\phi\,\mathrm du$. Since $\phi$ is constant along the whole ribbon at a given (frozen) extension $x$, integrating this from the base end ($u=0$) to the active end ($u=L$) gives a total axial displacement of $L\sin\phi$. By definition, this total axial displacement between the two ends of the ribbon is exactly $x$, so
\begin{equation}\label{eq:phi}
    x = L\sin\phi \qquad\Longrightarrow\qquad \phi = \sin^{-1}\left(\frac{x}{L}\right).
\end{equation}
Likewise, $\hat u$ has $\theta$-component $\cos\phi$, so moving a distance $\mathrm du$ along the ribbon produces a circumferential arc length $r\,\mathrm d\theta = \cos\phi\,\mathrm du$. Integrating over the whole ribbon length $L$, the ribbon sweeps out a total circumferential arc length $L\cos\phi$. This total arc length must also equal $r\theta_L$ (arc length equals radius times swept angle), since by construction the ribbon winds through the fixed total angle $\theta_L$ regardless of $x$. Equating the two expressions for this arc length and using $\cos\phi=\sqrt{1-\sin^2\phi}$ together with Eq.~\eqref{eq:phi} gives
\begin{equation}\label{eq:r}
    r\theta_L = L\cos\phi \qquad\Longrightarrow\qquad r = \frac{L}{\theta_L}\cos\phi = \frac{L}{\theta_L}\sqrt{1-\frac{x^2}{L^2}} = \frac{\sqrt{L^2 - x^2}}{\theta_L}.
\end{equation}

\subsection{Position vector}\label{sec:position}
An area element $du \times dv$ at point $(u,v)$ has a position vector given in cylindrical coordinates. We build this up from Eq.~\eqref{eq:frame_rotation}: since $\hat u = \cos\phi\,\hat\theta+\sin\phi\,\hat z$, moving along the ribbon by $\mathrm du$ produces $r\,\mathrm d\theta = \cos\phi\,\mathrm du$ and $\mathrm dz = \sin\phi\,\mathrm du$; since $\hat v = -\sin\phi\,\hat\theta + \cos\phi\,\hat z$, moving across the width by $\mathrm dv$ produces $r\,\mathrm d\theta = -\sin\phi\,\mathrm dv$ and $\mathrm dz=\cos\phi\,\mathrm dv$. Integrating these (at fixed, frozen $x$, so $r$ and $\phi$ are constants) from the reference point $(u,v)=(0,0)$ gives
\begin{equation}\label{eq:position}
    \vec{p}(u,v,x) =
    \begin{bmatrix}
        r\\
        \theta\\
        z
    \end{bmatrix} =
    \begin{bmatrix}
        r\\
        \frac{u \cos\phi - v \sin\phi}{r}\\
        u \sin\phi + v \cos\phi
    \end{bmatrix}.
\end{equation}

\subsection{Local curvature}\label{sec:local_curvature}
A cylinder of radius $r$ has two principal curvatures: $\kappa_\theta = 1/r$ in the circumferential ($\hat\theta$) direction in which it is rolled, and $\kappa_z = 0$ in the axial ($\hat z$) direction in which it is completely flat. Equivalently, the standard cylindrical basis vectors rotate with $\theta$ at the rates
\begin{equation}\label{eq:cyl_basis_deriv}
    \frac{\partial\hat r}{\partial\theta} = \hat\theta, \qquad
    \frac{\partial\hat\theta}{\partial\theta} = -\hat r, \qquad
    \frac{\partial\hat z}{\partial\theta} = 0,
\end{equation}
(the first two follow from writing $\hat r=\cos\theta\,\hat x+\sin\theta\,\hat y$ and $\hat\theta=-\sin\theta\,\hat x+\cos\theta\,\hat y$ in terms of the fixed Cartesian basis and differentiating; the third holds because $\hat z$ does not depend on $\theta$ at all --- this is exactly the statement $\kappa_z=0$).

To differentiate the ribbon's own frame $(\hat u,\hat v,\hat r)$ with respect to $(u,v)$ at a fixed $x$ (and therefore a fixed $r,\phi$), we take the partial derivatives of the $\theta$ component of Eq.~\eqref{eq:position}:
\begin{equation}\label{eq:dtheta}
    \frac{\partial\theta}{\partial u} = \frac{\cos\phi}{r}, \qquad \frac{\partial\theta}{\partial v} = -\frac{\sin\phi}{r}.
\end{equation}
Combining Eq.~\eqref{eq:cyl_basis_deriv} and Eq.~\eqref{eq:dtheta} via the chain rule (e.g.\ $\frac{\partial\hat r}{\partial u} = \frac{\partial\hat r}{\partial\theta}\frac{\partial\theta}{\partial u}$) gives
\begin{equation}\label{eq:cyl_basis_uv}
    \frac{\partial\hat r}{\partial u} = \frac{\cos\phi}{r}\hat\theta, \qquad
    \frac{\partial\hat r}{\partial v} = -\frac{\sin\phi}{r}\hat\theta, \qquad
    \frac{\partial\hat\theta}{\partial u} = -\frac{\cos\phi}{r}\hat r, \qquad
    \frac{\partial\hat\theta}{\partial v} = \frac{\sin\phi}{r}\hat r,
\end{equation}
with $\hat z$ constant with respect to $u,v$. Now we take the partial derivatives of $\hat u,\hat v$ themselves, using Eq.~\eqref{eq:frame_rotation} with $\phi$ held fixed:
\begin{equation}\label{eq:duv_raw}
    \begin{aligned}
    \frac{\partial\hat u}{\partial u} &= \cos\phi\,\frac{\partial\hat\theta}{\partial u} = -\frac{\cos^2\phi}{r}\hat r, &
    \frac{\partial\hat u}{\partial v} &= \cos\phi\,\frac{\partial\hat\theta}{\partial v} = \frac{\sin\phi\cos\phi}{r}\hat r, \\
    \frac{\partial\hat v}{\partial u} &= -\sin\phi\,\frac{\partial\hat\theta}{\partial u} = \frac{\sin\phi\cos\phi}{r}\hat r, &
    \frac{\partial\hat v}{\partial v} &= -\sin\phi\,\frac{\partial\hat\theta}{\partial v} = -\frac{\sin^2\phi}{r}\hat r.
    \end{aligned}
\end{equation}
The same three coefficients, $\cos^2\phi/r$, $\sin^2\phi/r$, and $\sin\phi\cos\phi/r$ (the last one twice), keep reappearing. We name them for convenience:
\begin{equation}\label{eq:curvatures}
    \kappa_{uu} := \frac{\cos^2\phi}{r}, \qquad
    \kappa_{vv} := \frac{\sin^2\phi}{r}, \qquad
    \kappa_{uv} := \frac{\sin\phi\cos\phi}{r},
\end{equation}
% so that Eq.~\eqref{eq:duv_raw} reads $\partial\hat u/\partial u=-\kappa_{uu}\hat r$, $\partial\hat u/\partial v=\kappa_{uv}\hat r$, $\partial\hat v/\partial u=\kappa_{uv}\hat r$, $\partial\hat v/\partial v=-\kappa_{vv}\hat r$.

% \paragraph{What $\kappa_{uv}$ means.} $\kappa_{uu}$ and $\kappa_{vv}$ are the ordinary curvatures of the $u$- and $v$-coordinate curves themselves: $\kappa_{uu}$ is how fast $\hat u$ tips toward $\hat r$ per unit distance moved \emph{along} $\hat u$, and likewise $\kappa_{vv}$ for $\hat v$ along $\hat v$. $\kappa_{uv}$ is instead a \emph{cross} rate: how fast $\hat u$ tips toward $\hat r$ per unit distance moved in the $v$-direction --- equivalently, how fast $\hat v$ tips toward $\hat r$ per unit distance moved in the $u$-direction, since Eq.~\eqref{eq:duv_raw} gives the same coefficient for both. It is not a slope like $\mathrm dv/\mathrm du$; it has the same units (one over length) and the same geometric meaning as $\kappa_{uu},\kappa_{vv}$ --- a rate at which a tangent direction tilts toward the surface normal --- just for a mixed pair of directions rather than a single one.

% Concretely, $\kappa_{uv}$ arises above because $\hat u,\hat v$ are each a mix of $\hat\theta$ and $\hat z$ (Eq.~\ref{eq:frame_rotation}), and only the $\hat\theta$ part of either vector rotates as $\theta$ changes ($\hat z$ does not, Eq.~\ref{eq:cyl_basis_deriv}). Moving in $v$ changes $\theta$ (Eq.~\ref{eq:dtheta}), and therefore rotates the $\hat\theta$-component of $\hat u$ toward $\hat r$ even though $v$ is not $\hat u$'s own direction --- that leftover rotation is exactly $\kappa_{uv}$. Put differently: $\hat u,\hat v$ are rotated by $\phi$ away from the cylinder's own principal directions $\hat\theta,\hat z$, which have no such cross term ($\hat\theta$ only ever rotates toward $\hat r$ as $\theta$ itself changes, never as $z$ changes, and $\hat z$ never rotates at all); $\kappa_{uv}$ is exactly the off-diagonal curvature this rotation introduces. Consistent with this, $\kappa_{uv}=\sin\phi\cos\phi/r$ vanishes at $\phi=0,90^\circ$ (where $\hat u,\hat v$ realign with $\hat\theta,\hat z$) and is largest at $\phi=45^\circ$.

Finally, we differentiate $\hat r$ itself. We already have $\frac{\partial\hat r}{\partial u},\frac{\partial\hat r}{\partial v}$ in terms of $\hat\theta$ (Eq.~\ref{eq:cyl_basis_uv}). To express them in terms of $\hat u,\hat v$ instead, we invert Eq.~\eqref{eq:frame_rotation} to obtain $\hat\theta=\cos\phi\,\hat u-\sin\phi\,\hat v$. Substituting this expression for $\hat \theta$ into Eq.~\eqref{eq:cyl_basis_uv} gives
\begin{align}
    \frac{\partial\hat r}{\partial u} = \frac{\cos\phi}{r}\big(\cos\phi\,\hat u-\sin\phi\,\hat v\big) &= \kappa_{uu}\hat u-\kappa_{uv}\hat v\\
    \frac{\partial\hat r}{\partial v} = -\frac{\sin\phi}{r}\big(\cos\phi\,\hat u-\sin\phi\,\hat v\big) &= -\kappa_{uv}\hat u+\kappa_{vv}\hat v
\end{align}
Collecting everything derived in this section,
\begin{equation}\label{eq:frame_rotation_rates}
    \begin{aligned}
    \frac{\partial\hat r}{\partial u} &= \kappa_{uu}\hat u - \kappa_{uv}\hat v, &
    \frac{\partial\hat r}{\partial v} &= -\kappa_{uv}\hat u + \kappa_{vv}\hat v, \\
    \frac{\partial\hat u}{\partial u} &= -\kappa_{uu}\hat r, &
    \frac{\partial\hat u}{\partial v} &= \kappa_{uv}\hat r, \\
    \frac{\partial\hat v}{\partial u} &= \kappa_{uv}\hat r, &
    \frac{\partial\hat v}{\partial v} &= -\kappa_{vv}\hat r.
    \end{aligned}
\end{equation}
% Two features of Eq.~\eqref{eq:frame_rotation_rates} are worth flagging because they are used repeatedly below: (i) $\hat u,\hat v$ never rotate into each other ($\partial\hat u/\partial u$ and $\partial\hat u/\partial v$ have no $\hat v$-component, and vice versa) --- this holds because the $u$- and $v$-coordinate lines are straight lines on the flat unrolled rectangle, hence remain geodesics after the isometric wrapping into a helix, and a curve's own tangent frame only rotates toward the surface normal along a geodesic, never within the tangent plane; (ii) the rotation matrix is antisymmetric, e.g.\ $\partial\hat u/\partial u\cdot\hat r=-\partial\hat r/\partial u\cdot\hat u$, as required for any rotating orthonormal frame.


\section{Stress balance}\label{sec:equilibrium}
We will model the paper yoyo as a thin shell of small but finite thickness $t\ll r$. Because the ribbon's thickness ultimately carries the boundary tractions responsible for friction (Section~\ref{sec:friction}), we work throughout with its full three-dimensional stress state. We use $\zeta\in[-t/2,t/2]$ for the coordinate measuring position through the thickness, i.e., displacement from the mid-surface along $\hat r$. At a point $(u,v,\zeta)$ the material carries the full Cauchy stress tensor:
\begin{equation}
    \sigma = \begin{bmatrix}\sigma_{uu}&\sigma_{uv}&\sigma_{ur}\\ \sigma_{uv}&\sigma_{vv}&\sigma_{vr}\\ \sigma_{ur}&\sigma_{vr}&\sigma_{rr}\end{bmatrix}
\end{equation}
There are six independent components: the in-plane stresses $\sigma_{uu},\sigma_{uv},\sigma_{vv}$; the transverse shears $\sigma_{ur},\sigma_{vr}$ by which the ribbon can transmit a load sideways from one part of its width to another, and the normal pressure $\sigma_{rr}$ caused by layer-layer contact at the ribbon's surface. 

We perform a force balance on a differential volume element $\mathrm du\times\mathrm dv\times\mathrm d\zeta$. Each direction has a corresponding traction vector:
\begin{equation}
\vec t_u=\sigma_{uu}\hat u+\sigma_{uv}\hat v+\sigma_{ur}\hat r \qquad
\vec t_v=\sigma_{uv}\hat u+\sigma_{vv}\hat v+\sigma_{vr}\hat r \qquad
\vec t_\zeta=\sigma_{ur}\hat u+\sigma_{vr}\hat v+\sigma_{rr}\hat r
\end{equation}
Because $t\ll r$, the element can be approximated as a rectangular prism, so the net force on the element is
\begin{equation}
    \left(\frac{\partial\vec t_u}{\partial u}+\frac{\partial\vec t_v}{\partial v}+\frac{\partial\vec t_\zeta}{\partial\zeta}\right)\mathrm du\,\mathrm dv\,\mathrm d\zeta 
    =0
\end{equation}
However, because $\hat u,\hat v,\hat r$ themselves depend on $u,v$ (Eq.~\ref{eq:frame_rotation_rates}), differentiating $\vec t_u,\vec t_v,\vec t_\zeta$ produces extra terms beyond the plain derivatives of the stress components. For example, expanding $\frac{\partial}{\partial u}( \sigma_{uu} \hat u)$ introduces a curvature-weighted term in the $\hat r$ direction:

\begin{equation}
    \frac{\partial}{\partial u} (\sigma_{uu} \hat u) = \frac{\partial \sigma_{uu}}{\partial u}\hat u + \sigma_{uu}\frac{\partial \hat u}{\partial u} 
    = \frac{\partial \sigma_{uu}}{\partial u}\hat u - \kappa_{uu}\sigma_{uu}\hat r
\end{equation}
The additional curvature-weighted terms reflect the fact that the element is not perfectly planar, so in-plane stresses are slightly rotated to have a small out-of-plane component, and vice versa. Expanding all terms and sorting by component gives three scalar force-balance equations:

\begin{align}
    \hat u:\quad &\boxed{\frac{\partial\sigma_{uu}}{\partial u}+\frac{\partial\sigma_{uv}}{\partial v}+\frac{\partial\sigma_{ur}}{\partial\zeta} + \kappa_{uu}\sigma_{ur} - \kappa_{uv}\sigma_{vr} = 0} \label{eq:inplane_u_full}\\
    \hat v:\quad &\boxed{\frac{\partial\sigma_{uv}}{\partial u}+\frac{\partial\sigma_{vv}}{\partial v}+\frac{\partial\sigma_{vr}}{\partial\zeta} - \kappa_{uv}\sigma_{ur} + \kappa_{vv}\sigma_{vr} = 0} \label{eq:inplane_v_full}\\
    \hat r:\quad &\boxed{\frac{\partial\sigma_{ur}}{\partial u}+\frac{\partial\sigma_{vr}}{\partial v}+\frac{\partial\sigma_{rr}}{\partial\zeta} - \kappa_{uu}\sigma_{uu}+2\kappa_{uv}\sigma_{uv}-\kappa_{vv}\sigma_{vv} = 0} \label{eq:outofplane_r_full}
\end{align}

Equilibrium alone only gives 3 equations for 6 unknown stress components. However, because $t$ is small, we will make a first-order approximation that the stress field varies linearlly through $t$. This means that for any stress component, if we know its boundary value at both $-\frac{t}{2}, \frac{t}{2}$ for all $(u,v)$, that component is essentially solved. In the following section, we will obtain the surface boundary conditions for $\sigma_{ur}, \sigma_{vr}$, and $\sigma_{rr}$ based on layer-layer contact.


\section{Friction}\label{sec:friction}
% Sections~\ref{sec:inplane_full}--\ref{sec:outofplane_full} left the boundary tractions at $\zeta=\pm t/2$ --- equivalently, the quantities $f_u,f_v$ of Eq.~\eqref{eq:N_balance} and $P_{in},P_{out}$ of Eq.~\eqref{eq:Pcontact_bc} --- entirely unspecified. We now determine them: first the slip direction based on the layer kinematics, and then their magnitude based on Coulomb friction applied to the contact pressure of Section~\ref{sec:outofplane_full}.

\subsection{Overlapping neighbors}\label{sec:neighbors}
Because the ribbon is wound around itself, a given area element at $(u,v)$ could physically overlap with another area elements one full turn away along the ribbon. Specifically, an area element at point $(u,v)$ overlaps with the neighboring interior layer at point $(u^-,v^-)$ if their cylindrical coordinates have equal $r$ and $z$, but differ by one full turn in $\theta$. We can express this by taking the difference in $\vec{p}$:
\begin{equation}
    \vec{p}(u,v,x) - \vec{p}(u^-,v^-,x) = \begin{bmatrix}
        0\\
        2\pi\\
        0
    \end{bmatrix}
\end{equation}
Plugging in Eq~\eqref{eq:position} gives:
\begin{align}
    u^- &= u - 2\pi r \cos \phi,\label{eq:uminus}\\
    v^- &= v + 2\pi r \sin \phi.\label{eq:vminus}
\end{align}
By the identical argument with $\theta(u^+,v^+)=\theta(u,v)+2\pi$ (i.e., the neighboring \emph{outer} layer, one full turn further out), the element has an outer overlapping neighbor at
\begin{align}
    u^+ &= u + 2\pi r \cos \phi,\label{eq:uplus}\\
    v^+ &= v - 2\pi r \sin \phi.\label{eq:vplus}
\end{align}

Some points $(u,v)$ on the surface overlap with an inner or outer layer, while others don't. We neglect the edge effects where a neighbor is absent because $u^-,u^+$ is outside the $[0,L]$ bounds. As a result, an inner neighbor is present exactly whenever $v^-$ lies within $[-W/2,W/2]$, and an outer neighbor is present exactly whenever $v^+$ lies in $[-W/2,W/2]$. That is,
\begin{align}
    \text{Inner neighbor is present if}&\quad v\le \frac{W}{2}-2\pi r \sin\phi\\
    \text{Outer neighbor is present if}&\quad v\ge -\frac{W}{2}+2\pi r \sin\phi
\end{align}


\subsection{Friction direction}\label{sec:slip_direction}
The position vector $\vec p(u,v,x)$ of a material point $(u,v)$ is given by Eq.~\eqref{eq:position}. Substituting Eq.~\eqref{eq:phi} and Eq.~\eqref{eq:r} to expose the dependencies on $x$ gives:
\begin{equation}
    \vec{p}(u,v,x) =
    \begin{bmatrix}
        \frac{\sqrt{L^2 -x^2 }}{\theta_L }\\
        \frac{u\theta_L}{L}-\frac{v\,x\,\theta_L}{L\sqrt{L^2 -x^2 }}\\
        \frac{ux}{L}+\frac{v\sqrt{L^2 -x^2 }}{L}
    \end{bmatrix}
\end{equation}
The velocity of a given material point $(u,v)$ as the active end of the ribbon is displaced (i.e., as $x$ changes) is then obtained by taking the partial derivative of each component of Eq.~\eqref{eq:position} with respect to $x$:

\begin{equation}\label{eq:velocity}
    \frac{\partial}{\partial x} \vec{p}(u,v,x) =
    \begin{bmatrix}
        \frac{\partial r}{\partial x}\\
        \frac{\partial \theta}{\partial x}\\
        \frac{\partial z}{\partial x}
    \end{bmatrix}=
    \begin{bmatrix}
        -\frac{x}{\theta_L \,\sqrt{L^2 -x^2 }}\\
        -\frac{L\,\theta_L\,v}{{\left(L^2-x^2\right)}^{3/2}}\\
        \frac{u}{L}-\frac{vx}{L\sqrt{L^2 -x^2 }}\\
    \end{bmatrix}.
\end{equation}

We can then plug $u^-,v^-$ into Eq.~\eqref{eq:velocity} to compute the velocity of the element relative to its inner overlapping neighbor, which gives the relative slip direction in the $r,\theta,z$ frame:
\begin{equation}
    \frac{\partial \vec{p}(u,v,x)}{\partial x} - \frac{\partial \vec{p}(u^-,v^-,x)}{\partial x} =
    \begin{bmatrix}
        0\\
        \frac{2\pi\,x}{L^2-x^2}\\
        \frac{2\pi}{\theta_L}
    \end{bmatrix}.
\end{equation}
As expected, there is no relative velocity in the radial direction (i.e., the layers are not lifting away from each other). We can now compute the relative slip direction in the local frame by projecting this slip velocity from the $\theta,z$ basis onto the the $u,v$ basis using the relation described by Eq.~\eqref{eq:frame_rotation}. Since $\frac{\partial\theta}{\partial x}$ above is an \emph{angular} rate, we first convert it to a linear (arc-length) rate by multiplying by $r$, giving $v_\theta = r\,\frac{\partial\theta}{\partial x}$; then projecting as just described:
\begin{equation}\label{eq:vslip_raw}
    \vec{v}_{slip}(u,v,x) = \begin{bmatrix}
        \cos\phi & \sin\phi \\
        -\sin\phi & \cos\phi
    \end{bmatrix}
    \begin{bmatrix}
        r\cdot\frac{2\pi\,x}{L^2-x^2}\\
        \frac{2\pi}{\theta_L}
    \end{bmatrix}
\end{equation}

We simplify this expression by substituting subsituting $x$ and $r$ in terms of $\phi$ (Eq.~\eqref{eq:phi},~\eqref{eq:r}) to expose the double-angle combination, then applying double angle trig identities to factor out common terms. The result is a scaled unit vector:
\begin{equation}
    \vec{v}_{slip}(u,v,x) = \frac{2\pi}{\theta_L\cos\phi} \begin{bmatrix} \sin(2\phi) \\ \cos(2\phi) \end{bmatrix}
\end{equation}
The prefactor is strictly positive for all $x \in (0,L)$. Therefore, the relative slip velocity points in the direction of the unit vector $-(\sin2\phi,\cos2\phi)$ in the local $u,v$ frame, so the Coulomb friction force opposing this motion must point in the opposite direction:
\begin{equation}\label{eq:friction_direction}
    \hat{f} = -\begin{bmatrix} \sin(2\phi) \\ \cos(2\phi) \end{bmatrix}
\end{equation}


\subsection{Friction magnitude}\label{sec:friction_magnitude}
Let us take a moment to introduce some shorthand notation:
\begin{align}
    \sigma_{rr}^\pm &:= \sigma_{rr}\left(\zeta=\pm\frac{t}{2}\right)\\
    \Delta \sigma_{rr}&:=\sigma_{rr}^+-\sigma_{rr}^-\\
    S_{rr}&:=\sigma_{rr}^++\sigma_{rr}^-
\end{align}

Recall that Eq.~\ref{eq:friction_direction} is the friction exerted on the element by the inner layer, which resists the element from sliding forward with $x$. The components of this friction force are equivalent to the shear stresses acting on the inner ($-\hat r$) face. Additionally, the magnitude of the Coulomb friction force is equivalent to the normal pressure $-\sigma_{rr}$ times a friction coefficient $\mu$. By saying this, we assume $\sigma_{rr}\le 0$ (the element is under compression), deferring a rigorous justification to Section~\ref{sec:sign_monotonicity}. 
\begin{align}
    \sigma_{ur}\left(\zeta = -\frac{t}{2}\right) &= -\mu \sigma_{rr}^-\sin(2\phi)\label{eq:sigma_ur_inner}\\
    \sigma_{vr}\left(\zeta = -\frac{t}{2}\right) &= -\mu \sigma_{rr}^-\cos(2\phi)\label{eq:sigma_vr_inner}
\end{align}
By the same argument, the friction exerted by the outer layer is the exact opposite, dragging the element forward. We then obtain a similar boundary relationship for the outer ($+\hat r$) face. Note that the sign is positive due to the sign convention for shear stresses on the inner face of an element.
\begin{align}
    \sigma_{ur}\left(\zeta = \frac{t}{2}\right) &= -\mu \sigma_{rr}^+\sin(2\phi)\label{eq:sigma_ur_outer}\\
    \sigma_{vr}\left(\zeta = \frac{t}{2}\right) &= -\mu \sigma_{rr}^+\cos(2\phi)\label{eq:sigma_vr_outer}
\end{align}

Let thickness-averaged stress components be denoted by a bar, i.e., $\bar\sigma_{ij} := \frac{1}{t}\int_{-\frac{t}{2}}^{\frac{t}{2}}\sigma_{ij}\,\mathrm d\zeta$. For simplicity, we assume the thickness is small enough that the variation of all stress components through the thickness can be approximated as linear. We can now use Eqs.~\ref{eq:sigma_ur_inner}--\ref{eq:sigma_vr_outer} to get the thickness-averaged values $\bar\sigma_{ur}$ and $\bar\sigma_{vr}$ in terms of the two boundary values of $\sigma_{rr}$:
\begin{align}
    \Aboxed{\bar\sigma_{ur} &= -\frac{\mu}{2}S_{rr}\sin(2\phi)}\label{eq:bar_sigma_ur}\\
    \Aboxed{\bar\sigma_{vr} &= -\frac{\mu}{2}S_{rr}\cos(2\phi)}\label{eq:bar_sigma_vr}
\end{align}

\subsection{Layer-layer contact relationship}\label{sec:layer_layer_contact}
By Newton's third law, two surfaces in contact should experience equal and opposite traction forces. Because the surface normals are opposite, this means they should have equal stresses. It suffices to equate the normal pressures $\sigma_{rr}$ because the transverse shear stresses are related above. Therefore, we get the following recursive relationships to define the boundary stresses, using piecewise gates based on layer contact boundaries:
\begin{align}
    \text{Outer surface:}&\quad \boxed{\sigma_{rr}^+\left(u,v\right) = \begin{cases}
        \sigma_{rr}^-\left(u^+,v^+\right) & \quad v \ge -\frac{W}{2}+2\pi r \sin\phi\\
        0 &\quad \text{otherwise}
    \end{cases}}\label{eq:contact_outer}\\
    \text{Inner surface:}&\quad \boxed{\sigma_{rr}^-\left(u,v\right) = \begin{cases}
        \sigma_{rr}^+\left(u^-,v^-\right) & \quad v \le \frac{W}{2}-2\pi r \sin\phi\\
        0 &\quad \text{otherwise}
    \end{cases}}\label{eq:contact_inner}\\
\end{align}
The above relationships recursively define the boundary stresses of the entire ribbon, using the boundary condition at the outermost layer $\sigma_{rr}(u=L) = 0$ as a base case. 


\section{PDE formulation and closed-form solution}\label{sec:closed_form}
We now assemble the pieces above into a closed system for the stress field. The three equilibrium equations, Eqs.~\eqref{eq:inplane_u_full}--\eqref{eq:outofplane_r_full}, are three PDEs for six unknown stress components at every point $(u,v,\zeta)$. Section~\ref{sec:friction} supplies what is needed to close this at the two surfaces $\zeta=\pm\frac{t}{2}$: there, $\sigma_{ur},\sigma_{vr}$ are fixed directly in terms of $\sigma_{rr}$ by Coulomb friction (Eqs.~\ref{eq:sigma_ur_inner}--\ref{eq:sigma_vr_outer}), and $\sigma_{rr}$'s own two boundary values are tied recursively, layer to layer, by Newton's third law (Eqs.~\ref{eq:contact_outer}--\ref{eq:contact_inner}). Combined with the approximation that the stress field varies linearly through the thickness, this eliminates the unknown $\sigma_{rr},\sigma_{ur},\sigma_{vr}$ components and reduces the remaining unknowns to a function of $(u,v)$ alone. 
% We also carry forward the free-edge boundary conditions on the long, unloaded edges of the ribbon,
% \begin{equation}\label{eq:bc_free_edges}
%     \boxed{\sigma_{uv}\left(u,v=\pm\frac{W}{2}\right) = 0, \qquad \sigma_{vv}\left(u,v=\pm\frac{W}{2}\right)=0,}
% \end{equation}
% which hold at every $\zeta$ and so apply directly to the thickness-uniform $\sigma_{uv},\sigma_{vv}$ used below, and the end load applied at the active end, $F(x)=W\big[\sigma_{uu}(L)\sin\phi+\sigma_{uv}(L)\cos\phi\big]$ (the resultant of $\sigma_{uu},\sigma_{uv}$ projected onto the global pulling direction).

\subsection{In-plane thickness integration}\label{sec:thickness_integration}
We now integrate the three equilibrium equations through the thickness. Beginning with the $\hat u$ direction Eq~\eqref{eq:inplane_u_full},
\begin{align}
    \frac{1}{t}\int_{-\frac{t}{2}}^{\frac{t}{2}}\left(\frac{\partial\sigma_{uu}}{\partial u}+\frac{\partial\sigma_{uv}}{\partial v}+\frac{\partial\sigma_{ur}}{\partial\zeta} + \kappa_{uu}\sigma_{ur}-\kappa_{uv}\sigma_{vr}\right)\,\mathrm d\zeta &= 0\\
    \frac{\partial\bar\sigma_{uu}}{\partial u} + \frac{\partial\bar\sigma_{uv}}{\partial v} + \frac{1}{t}\sigma_{ur}\Big|_{-\frac{t}{2}}^{\frac{t}{2}}  + \kappa_{uu}\bar\sigma_{ur} - \kappa_{uv}\bar\sigma_{vr} &= 0
\end{align}
Then substituting Eqs.~\eqref{eq:bar_sigma_ur}--\eqref{eq:bar_sigma_vr} for the thickness-averaged transverse shear stresses, and expanding the trig identities in the curvature terms:
\begin{equation}
    \frac{\partial\bar\sigma_{uu}}{\partial u} + \frac{\partial\bar\sigma_{uv}}{\partial v} - \frac{\mu \sin(2\phi)}{t}\left(\sigma_{rr}^+ -\sigma_{rr}^-\right) + \frac{\mu}{2}S_{rr}\left(\kappa_{uv}\cos(2\phi)-\kappa_{uu}\sin(2\phi)\right) = 0
\end{equation}
\begin{equation}\label{eq:inplane_u_integrated}
    \frac{\partial\bar\sigma_{uu}}{\partial u} + \frac{\partial\bar\sigma_{uv}}{\partial v} - \frac{\mu \sin(2\phi)\Delta\sigma_{rr}}{t} - \frac{\mu\sin(2\phi)S_{rr}}{4r} = 0
\end{equation}
Integrating the $\hat v$ direction Eq~\eqref{eq:inplane_v_full} gives a similar result:
\begin{equation}\label{eq:inplane_v_integrated}
    \frac{\partial\bar\sigma_{uv}}{\partial u} + \frac{\partial\bar\sigma_{vv}}{\partial v} - \frac{\mu \cos(2\phi)\Delta \sigma_{rr}}{t} + \frac{\mu\cos(2\phi)S_{rr}}{4r} = 0
\end{equation}
%TODO: verify these two integrated equations


\subsection{Out-of-plane thickness integration}\label{sec:thickness_integration_outofplane}
Next, we integrate the $\hat r$ direction Eq~\eqref{eq:outofplane_r_full}:
\begin{align}
    \frac{1}{t}\int_{-\frac{t}{2}}^{\frac{t}{2}}\left(\frac{\partial\sigma_{ur}}{\partial u}+\frac{\partial\sigma_{vr}}{\partial v}+\frac{\partial\sigma_{rr}}{\partial\zeta} - \kappa_{uu}\sigma_{uu}+2\kappa_{uv}\sigma_{uv}-\kappa_{vv}\sigma_{vv}\right)\,\mathrm d\zeta &= 0\\
    \frac{\partial\bar\sigma_{ur}}{\partial u} + \frac{\partial\bar\sigma_{vr}}{\partial v} + \frac{1}{t}\Delta \sigma_{rr} - \kappa_{uu}\bar\sigma_{uu}+2\kappa_{uv}\bar\sigma_{uv}-\kappa_{vv}\bar\sigma_{vv} &= 0
\end{align}
Substituting Eqs.~\eqref{eq:bar_sigma_ur}--\eqref{eq:bar_sigma_vr} for $\bar\sigma_{ur},\bar\sigma_{vr}$ gives,
\begin{equation}\label{eq:outofplane_exact}
    \boxed{\Delta \sigma_{rr} = t\big[\kappa_{uu}\sigma_{uu}-2\kappa_{uv}\sigma_{uv}+\kappa_{vv}\sigma_{vv}\big] + \frac{t\mu}{2}\left[\sin(2\phi)\frac{\partial S}{\partial u}+\cos(2\phi)\frac{\partial S}{\partial v}\right].}
\end{equation}
This is one equation in the two unknowns $S_{rr},\Delta\sigma_{rr}$ (equivalently, $\sigma_{rr}^+,\sigma_{rr}^-$); by itself it is not yet closed. We now bring in the recursive contact relationship, Eqs.~\eqref{eq:contact_outer}--\eqref{eq:contact_inner}, the same way Section~\ref{sec:accumulated_pressure} did for $\sigma_{rr}^+$ alone: Taylor-expand $\sigma_{rr}^-(u^+,v^+)$ about $(u,v)$ in the fixed shift $(2\pi r\cos\phi,-2\pi r\sin\phi)$,
\begin{equation}
    \sigma_{rr}^+(u,v) = \sigma_{rr}^-(u^+,v^+) \approx \sigma_{rr}^-(u,v) + 2\pi r\cos\phi\,\frac{\partial\sigma_{rr}^-}{\partial u} - 2\pi r\sin\phi\,\frac{\partial\sigma_{rr}^-}{\partial v},
\end{equation}
and, since $\sigma_{rr}^+-\sigma_{rr}^-$ is already the small quantity being solved for, replace $\partial\sigma_{rr}^-/\partial u,\partial\sigma_{rr}^-/\partial v$ by $\frac12\partial S/\partial u,\frac12\partial S/\partial v$ at leading order (dropping the $O(\sigma_{rr}^+-\sigma_{rr}^-)$ correction to this replacement itself):
\begin{equation}\label{eq:contact_exact}
    \boxed{\sigma_{rr}^+-\sigma_{rr}^- \approx \pi r\cos\phi\,\frac{\partial S}{\partial u} - \pi r\sin\phi\,\frac{\partial S}{\partial v}.}
\end{equation}
Equations~\eqref{eq:outofplane_exact} and~\eqref{eq:contact_exact} are two independent expressions for the same quantity, $\sigma_{rr}^+-\sigma_{rr}^-$, both written in terms of $\partial S/\partial u,\partial S/\partial v$. Equating them,
\begin{equation}
    \left(\pi r\cos\phi - \frac{t\mu}{2}\sin(2\phi)\right)\frac{\partial S}{\partial u} - \left(\pi r\sin\phi+\frac{t\mu}{2}\cos(2\phi)\right)\frac{\partial S}{\partial v} = t\big[\kappa_{uu}\sigma_{uu}-2\kappa_{uv}\sigma_{uv}+\kappa_{vv}\sigma_{vv}\big],
\end{equation}
the $t\mu$ corrections to the two coefficients are smaller than the leading $\pi r\cos\phi,\pi r\sin\phi$ terms by a factor of order $t/r$ (comparing an explicit $t$ against an explicit $r$), so at leading order they drop, leaving the closed transport equation
\begin{equation}\label{eq:S_transport}
    \boxed{\pi r\cos\phi\,\frac{\partial S}{\partial u} - \pi r\sin\phi\,\frac{\partial S}{\partial v} = t\big[\kappa_{uu}\sigma_{uu}-2\kappa_{uv}\sigma_{uv}+\kappa_{vv}\sigma_{vv}\big]}
\end{equation}
for $S$ alone --- exactly twice Eq.~\eqref{eq:pressure_transport} written for $\sigma_{rr}^+$, consistent with $S\approx2\sigma_{rr}^+$ once $\sigma_{rr}^+-\sigma_{rr}^-$ is small. Under the width-uniform ansatz of Section~\ref{sec:width_reduction} ($S$ independent of $v$), integrating Eq.~\eqref{eq:S_transport} along its characteristic out to $u'=L$ gives
\begin{equation}
    S(u) = S(L) - \frac{t}{\pi r\cos\phi}\int_u^L\big[\kappa_{uu}\sigma_{uu}(u')-2\kappa_{uv}\sigma_{uv}(u')+\kappa_{vv}\sigma_{vv}(u')\big]\,\mathrm du'.
\end{equation}
The boundary value $S(L)$ is fixed using $\sigma_{rr}^+(L,v)=0$ (Section~\ref{sec:layer_layer_contact}) together with Eq.~\eqref{eq:outofplane_exact}, which at $u=L$ (dropping its own $t\mu$ term, consistently) gives $\sigma_{rr}^+(L)-\sigma_{rr}^-(L)\approx t\big[\kappa_{uu}\sigma_{uu}-2\kappa_{uv}\sigma_{uv}+\kappa_{vv}\sigma_{vv}\big](L)$, so $S(L)=\sigma_{rr}^+(L)+\sigma_{rr}^-(L)=-t\big[\kappa_{uu}\sigma_{uu}-2\kappa_{uv}\sigma_{uv}+\kappa_{vv}\sigma_{vv}\big](L)$. Recovering $\sigma_{rr}^+=\frac12(S+(\sigma_{rr}^+-\sigma_{rr}^-))$, with the difference again given by Eq.~\eqref{eq:outofplane_exact} at leading order, gives the corrected closed form
\begin{equation}\label{eq:pressure_closed_corrected}
    \boxed{
    \sigma_{rr}^+(u) = -\frac{t}{2\pi r\cos\phi}\int_u^L\big[\kappa_{uu}\sigma_{uu}-2\kappa_{uv}\sigma_{uv}+\kappa_{vv}\sigma_{vv}\big](u')\,\mathrm du' \;+\; \frac{t}{2}\Big[\kappa_{uu}\sigma_{uu}-2\kappa_{uv}\sigma_{uv}+\kappa_{vv}\sigma_{vv}\Big]\Big|_u^L,
    }
\end{equation}
where $[\cdots]|_u^L$ denotes the bracket evaluated at $u'=L$ minus its value at $u'=u$. The first term reproduces Eq.~\eqref{eq:pressure_closed} exactly; the second is the new piece surfaced by keeping (rather than dropping) the bracket in Eq.~\eqref{eq:outofplane_integrated}. It carries the same explicit factor of $t$ as the first term but no compensating $1/r$, so it is smaller than the first term by a factor of order $r/L$ (the number of turns) --- small in the many-turns regime already assumed elsewhere, but now a derived, quantified correction rather than an asserted one.
%TODO: continue this derivation, use the integrated out of plane force balance + recursive contact relationship to get a closed form solution for sigma_rr


% Integrate Eq.~\eqref{eq:inplane_u_full} over $\zeta\in[-\frac{t}{2},\frac{t}{2}]$. Writing $\sigma_{ur}(u,v,\zeta)$ as linear in $\zeta$ with boundary values fixed by Eqs.~\eqref{eq:sigma_ur_inner},\eqref{eq:sigma_ur_outer}, its thickness average is
% $$\bar\sigma_{ur}:=\frac1t\int_{-\frac{t}{2}}^{\frac{t}{2}}\sigma_{ur}\,\mathrm d\zeta=\frac12\Big[\sigma_{ur}(\frac{t}{2})+\sigma_{ur}(-\frac{t}{2})\Big]$$
% (the linear part of the profile integrates to zero over the symmetric interval), while $\int\frac{\partial\sigma_{ur}}{\partial\zeta}\,\mathrm d\zeta=\sigma_{ur}(\frac{t}{2})-\sigma_{ur}(-\frac{t}{2})=:f_u(u,v)$ is the same boundary-flux difference used previously. Taking $\sigma_{uu},\sigma_{uv}$ uniform through the thickness (nothing at this order sources a $\zeta$-dependence in them) and dividing by $t$,
% \begin{equation}\label{eq:inplane_u_integrated}
%     \boxed{\frac{\partial\sigma_{uu}}{\partial u}+\frac{\partial\sigma_{uv}}{\partial v}+\frac{f_u}{t} + \kappa_{uu}\bar\sigma_{ur}-\kappa_{uv}\bar\sigma_{vr} = 0,}
% \end{equation}
% and, by the identical argument applied to Eq.~\eqref{eq:inplane_v_full},
% \begin{equation}\label{eq:inplane_v_integrated}
%     \boxed{\frac{\partial\sigma_{uv}}{\partial u}+\frac{\partial\sigma_{vv}}{\partial v}+\frac{f_v}{t} - \kappa_{uv}\bar\sigma_{ur}+\kappa_{vv}\bar\sigma_{vr} = 0.}
% \end{equation}
% Unlike a treatment that discards the curvature-weighted terms outright, Eqs.~\eqref{eq:inplane_u_integrated}--\eqref{eq:inplane_v_integrated} keep them, now expressed through the through-thickness \emph{average} $\bar\sigma_{ur},\bar\sigma_{vr}$ rather than the pointwise value. Using Eqs.~\eqref{eq:sigma_ur_inner}--\eqref{eq:sigma_vr_outer}, $\bar\sigma_{ur}=-\tfrac\mu2\sin(2\phi)\big[\sigma_{rr}(\frac{t}{2})+\sigma_{rr}(-\frac{t}{2})\big]$ and $\bar\sigma_{vr}=-\tfrac\mu2\cos(2\phi)\big[\sigma_{rr}(\frac{t}{2})+\sigma_{rr}(-\frac{t}{2})\big]$ (each term vanishing on whichever side has no neighbor), and a short calculation from Eq.~\eqref{eq:curvatures} shows $\kappa_{uu}\sin2\phi-\kappa_{uv}\cos2\phi=\kappa_{uv}$, so the curvature-weighted correction in Eq.~\eqref{eq:inplane_u_integrated} collapses to
% \begin{equation}\label{eq:curvature_correction}
%     \kappa_{uu}\bar\sigma_{ur}-\kappa_{uv}\bar\sigma_{vr} = -\frac{\mu\kappa_{uv}}{2}\Big[\sigma_{rr}(\frac{t}{2})+\sigma_{rr}(-\frac{t}{2})\Big],
% \end{equation}
% proportional to the \emph{sum}, rather than the difference, of the two boundary pressures --- and to $\kappa_{uv}$, not $\kappa_{uu}$. Because $\kappa_{uv}$ vanishes at $\phi=0,90^\circ$ and is exactly the off-diagonal curvature responsible for load transfer between $u$ and $v$ (Section~\ref{sec:local_curvature}), this term is a genuine, if secondary, correction to the friction-driven balance --- not something to be dropped outright --- and we carry it forward symbolically, returning to its relative size once $\sigma_{rr}$'s boundary values are pinned down below.

% We now integrate the out-of-plane equation, Eq.~\eqref{eq:outofplane_r_full}, the same way. Its $\zeta$-derivative term integrates directly to the \emph{jump} in $\sigma_{rr}$ across the thickness, $\Delta\sigma_{rr}:=\sigma_{rr}(\frac{t}{2})-\sigma_{rr}(-\frac{t}{2})$, while $\sigma_{uu},\sigma_{uv},\sigma_{vv}$ (uniform through the thickness) simply pick up a factor of $t$:
% \begin{equation}\label{eq:outofplane_integrated}
%     \boxed{\Delta\sigma_{rr} = t\big[\kappa_{uu}\sigma_{uu}-2\kappa_{uv}\sigma_{uv}+\kappa_{vv}\sigma_{vv}\big] - t\left[\frac{\partial\bar\sigma_{ur}}{\partial u}+\frac{\partial\bar\sigma_{vr}}{\partial v}\right],}
% \end{equation}
% where the second bracket involves an extra in-plane derivative of an already boundary-traction-sized quantity and is subdominant to the first; we drop it below and return to it only if the leading balance proves insufficient. Equation~\eqref{eq:outofplane_integrated} is the relation this section has been building toward: it says the two contact pressures felt on a ribbon element's two faces cannot be equal in general, but must differ by an amount set directly by the local in-plane tension, weighted by curvature and by the actual physical thickness $t$ --- exactly the capstan-type mechanism (normal pressure $\sim$ tension over radius, for a tensioned band pressed against a curved surface) that generates contact pressure from tension in the first place. Writing out $\Delta\sigma_{rr}=\sigma_{rr}(\frac{t}{2})-\sigma_{rr}(-\frac{t}{2})$ explicitly, Eq.~\eqref{eq:outofplane_integrated} becomes
% \begin{equation}\label{eq:pressure_jump}
%     \boxed{\sigma_{rr}(u,v,\frac{t}{2}) - \sigma_{rr}(u,v,-\frac{t}{2}) \approx t\Big[\kappa_{uu}\sigma_{uu}(u,v)-2\kappa_{uv}\sigma_{uv}(u,v)+\kappa_{vv}\sigma_{vv}(u,v)\Big].}
% \end{equation}

% \subsection{Closing the system: why $\sigma_{rr}$ does not simply propagate to zero}\label{sec:pressure_recursion}
% Read on its own, the matching condition of Section~\ref{sec:layer_layer_contact} (Eqs.~\ref{eq:contact_outer}--\ref{eq:contact_inner}) contains no mechanism for generating a nonzero contact pressure anywhere: starting from the base case $\sigma_{rr}(L,v,\frac{t}{2})=0$, it would simply propagate that zero value backward through every layer, forcing $\sigma_{rr}(u,v,\pm \frac{t}{2})\equiv0$ everywhere, and hence, through Eqs.~\eqref{eq:sigma_ur_inner}--\eqref{eq:sigma_vr_outer}, $\sigma_{ur}\equiv\sigma_{vr}\equiv0$ as well. It is Eq.~\eqref{eq:pressure_jump} that resolves this: the matching condition and the pressure-jump relation hold \emph{simultaneously}, not one after the other. Evaluating Eq.~\eqref{eq:pressure_jump} at $(u^+,v^+)$ and substituting $\sigma_{rr}(u^+,v^+,-\frac{t}{2})=\sigma_{rr}(u,v,\frac{t}{2})$ (Eq.~\ref{eq:contact_outer}) gives
% \begin{equation}\label{eq:pressure_recursion_full}
%     \boxed{\begin{aligned}
%     \sigma_{rr}(u,v,\tfrac{t}{2}) &= \sigma_{rr}(u^+,v^+,\tfrac{t}{2}) - t\Big[\kappa_{uu}\sigma_{uu}-2\kappa_{uv}\sigma_{uv}+\kappa_{vv}\sigma_{vv}\Big](u^+,v^+), \\
%     &\text{valid for } v\ge -\tfrac{W}{2}+2\pi r\sin\phi,
%     \end{aligned}}
% \end{equation}
% with $\sigma_{rr}(L,v,\frac{t}{2})=0$ starting the recursion. Unlike the matching condition alone, Eq.~\eqref{eq:pressure_recursion_full} carries a genuine source term at every step, $-t\big[\kappa_{uu}\sigma_{uu}-2\kappa_{uv}\sigma_{uv}+\kappa_{vv}\sigma_{vv}\big]$, nonzero whenever the local in-plane tension is nonzero. Unwound from the base case, $\sigma_{rr}(u,v,\frac{t}{2})$ is a sum of such terms evaluated at every wound layer further out than $(u,v)$ --- so $\sigma_{rr}$ accumulates precisely because, and only because, $\sigma_{uu}$ (and $\sigma_{uv},\sigma_{vv}$) are nonzero, exactly as anticipated. And $\sigma_{uu}$ is nonzero in turn because the Instron pull at the active end prescribes a nonzero resultant force $F(x)$ there (Section~\ref{sec:closed_form}, opening): it is that end condition, not anything internal to Eqs.~\eqref{eq:inplane_u_integrated}--\eqref{eq:pressure_jump}, that ultimately keeps every stress component in the system from relaxing to zero. (One can check directly that Eq.~\eqref{eq:contact_inner} combined with Eq.~\eqref{eq:pressure_jump} at $(u,v)$ reproduces exactly Eq.~\eqref{eq:pressure_recursion_full} shifted by one layer, so the two contact relations of Section~\ref{sec:layer_layer_contact} are consistent with a single underlying recursion, not two independent ones.)

% This also gives the complementary limit directly. If the helix is wound loosely enough that no $v$ satisfies either overlap condition at the current $x$ (Eq.~\ref{eq:overlap_in}--\ref{eq:overlap_out}), then $\sigma_{rr}(u,v,\pm \frac{t}{2})\equiv0$ is forced pointwise \emph{by definition} rather than by Eq.~\eqref{eq:pressure_recursion_full}, and Eq.~\eqref{eq:pressure_jump} then \emph{requires} $\kappa_{uu}\sigma_{uu}-2\kappa_{uv}\sigma_{uv}+\kappa_{vv}\sigma_{vv}\equiv0$ as a consistency condition. Together with $f_u=f_v=0$ in Eqs.~\eqref{eq:inplane_u_integrated}--\eqref{eq:inplane_v_integrated} and the free-edge conditions of Eq.~\eqref{eq:bc_free_edges}, this drives $\sigma_{uu},\sigma_{uv},\sigma_{vv}\equiv0$ as well: with no self-contact anywhere there is no mechanism to resist the pull, so the ribbon offers no static tension and simply slides freely. This matches the physical expectation exactly.

% \subsection{The accumulated contact pressure in closed form}\label{sec:accumulated_pressure}
% Equation~\eqref{eq:pressure_recursion_full} steps $(u,v)\to(u^+,v^+)=(u+2\pi r\cos\phi,\,v-2\pi r\sin\phi)$ at each iteration --- a fixed shift, since $r,\phi$ are frozen at any given extension $x$. Because the source term on the right of Eq.~\eqref{eq:pressure_recursion_full} carries an explicit factor of $t$, the change in $\sigma_{rr}(\cdot,\frac{t}{2})$ from one wound layer to the next is small (order $\frac{t}{r}$ relative to $\sigma_{uu}$ itself), even though the geometric step $2\pi r\cos\phi$ between layers is not. This is exactly the situation in which a discrete recursion is well approximated by its continuum limit: treating $\sigma_{rr}(u,v,\frac{t}{2})$ as a smooth function and expanding Eq.~\eqref{eq:pressure_recursion_full} to first order in the step gives the transport equation
% \begin{equation}\label{eq:pressure_transport}
%     2\pi r\cos\phi\,\frac{\partial\sigma_{rr}(u,v,\frac{t}{2})}{\partial u} - 2\pi r\sin\phi\,\frac{\partial\sigma_{rr}(u,v,\frac{t}{2})}{\partial v} = t\Big[\kappa_{uu}\sigma_{uu}-2\kappa_{uv}\sigma_{uv}+\kappa_{vv}\sigma_{vv}\Big].
% \end{equation}
% The left side is exactly $2\pi r\cos\phi$ times the derivative of $\sigma_{rr}(\cdot,\frac{t}{2})$ along the direction $(\cos\phi,-\sin\phi)$ --- precisely the direction of the neighbor shift $(u^+,v^+)$ itself, per Eq.~\eqref{eq:uplus}--\eqref{eq:vplus}. If, as adopted below (Section~\ref{sec:width_reduction}), $\sigma_{uu},\sigma_{uv},\sigma_{vv}$ are taken independent of $v$, the right side of Eq.~\eqref{eq:pressure_transport} depends only on $u$, and integrating Eq.~\eqref{eq:pressure_transport} along its characteristic line from $(u,v)$ out to the base case at $u'=L$ gives the closed form
% \begin{equation}\label{eq:pressure_closed}
%     \boxed{\sigma_{rr}(u,v,\frac{t}{2}) = -\frac{t}{2\pi r\cos\phi}\int_u^L \Big[\kappa_{uu}\sigma_{uu}(u')-2\kappa_{uv}\sigma_{uv}(u')+\kappa_{vv}\sigma_{vv}(u')\Big]\,\mathrm du',}
% \end{equation}
% with $\sigma_{rr}(u,v,-\frac{t}{2})$ recovered directly from Eq.~\eqref{eq:pressure_jump}. Notably, Eq.~\eqref{eq:pressure_closed} is then independent of $v$ as well, consistent with the $v$-independence assumed for $\sigma_{uu},\sigma_{uv},\sigma_{vv}$ on the right. This closed form implicitly assumes the characteristic through $(u,v)$ reaches $u'=L$ before its $v$-coordinate drifts outside $\left[-\frac{W}{2},\frac{W}{2}\right]$ and exits through the side of the ribbon rather than its end; resolving that width-edge truncation exactly would replace the upper limit $L$ with $\min\!\left(L,\ u+\big(v+\frac{W}{2}\big)\frac{\cos\phi}{\sin\phi}\right)$, a secondary correction we do not carry through below.

% \subsection{Narrow-ribbon reduction and the closed ODE}\label{sec:width_reduction}
% As in a beam or narrow-plate approximation, and consistent with $L\gg W$ already assumed throughout, we take $\sigma_{vv}(u,v)\approx0$ and $\sigma_{uu},\sigma_{uv}$ approximately independent of $v$ (uniform across the width) for the remainder of this section; unlike the compatibility-based argument used in an earlier pass on this document, this is now adopted directly as a modeling approximation for a long, narrow ribbon, not derived. Integrating Eq.~\eqref{eq:inplane_u_integrated} over $v\in\left[-\frac{W}{2},\frac{W}{2}\right]$: the $\frac{\partial\sigma_{uu}}{\partial u}$ term gives $W\,\frac{\mathrm d\sigma_{uu}}{\mathrm du}$ (an ordinary derivative, since $\sigma_{uu}$ is now a function of $u$ alone); the $\frac{\partial\sigma_{uv}}{\partial v}$ term integrates, via the free-edge condition of Eq.~\eqref{eq:bc_free_edges}, to $\sigma_{uv}\left(u,\frac{W}{2}\right)-\sigma_{uv}\left(u,-\frac{W}{2}\right)=0$; and the $\frac{f_u}{t}$ term integrates using Eq.~\eqref{eq:sigma_ur_inner},\eqref{eq:sigma_ur_outer} and the fact that $\sigma_{rr}\left(u,\cdot,\pm\frac{t}{2}\right)$ is itself $v$-independent (Eq.~\ref{eq:pressure_closed}), so only the presence/absence gates of Eq.~\eqref{eq:overlap_in}--\eqref{eq:overlap_out} depend on $v$, each contributing a factor equal to the length of its band, $W-2\pi r\sin\phi$:
% \begin{equation}\label{eq:fu_width_integrated}
%     \begin{aligned}
%     \frac{1}{tW}\int_{-\frac{W}{2}}^{\frac{W}{2}} f_u\,\mathrm dv &= -\frac{\mu\sin(2\phi)\,(W-2\pi r\sin\phi)}{tW}\left[\sigma_{rr}\left(u,\frac{t}{2}\right)-\sigma_{rr}\left(u,-\frac{t}{2}\right)\right] \\
%     &= -\frac{\mu(W-2\pi r\sin\phi)}{W}\sin(2\phi)\Big[\kappa_{uu}\sigma_{uu}-2\kappa_{uv}\sigma_{uv}\Big],
%     \end{aligned}
% \end{equation}
% using Eq.~\eqref{eq:pressure_jump} (with $\sigma_{vv}\approx0$) for the bracketed jump. The curvature-correction term of Eq.~\eqref{eq:curvature_correction} instead needs the \emph{sum} $\sigma_{rr}\left(u,\frac{t}{2}\right)+\sigma_{rr}\left(u,-\frac{t}{2}\right)$, not the difference --- and by Eq.~\eqref{eq:pressure_closed} that sum is set by the pressure \emph{accumulated over every layer out to $u=L$}, not by the local tension at $u$ alone. Whether this nonlocal term is actually small compared to Eq.~\eqref{eq:fu_width_integrated} therefore depends on how many layers are wound outside $u$ (it is not guaranteed small just because $t\ll r$); we drop it here to obtain a closed, local system, flagging it --- honestly, not by assumption --- as an open refinement, exactly as with the term already dropped in Eq.~\eqref{eq:outofplane_integrated}. Applying the identical steps to Eq.~\eqref{eq:inplane_v_integrated} gives the pair
% \begin{equation}\label{eq:ode_final}
%     \boxed{\begin{aligned}
%     \frac{\mathrm d\sigma_{uu}}{\mathrm du} &= \frac{\mu(W-2\pi r\sin\phi)}{W}\sin(2\phi)\Big[\kappa_{uu}\sigma_{uu}-2\kappa_{uv}\sigma_{uv}\Big], \\
%     \frac{\mathrm d\sigma_{uv}}{\mathrm du} &= \frac{\mu(W-2\pi r\sin\phi)}{W}\cos(2\phi)\Big[\kappa_{uu}\sigma_{uu}-2\kappa_{uv}\sigma_{uv}\Big].
%     \end{aligned}}
% \end{equation}
% (For $2\pi r\sin\phi>W$ --- full overlap, no bare band at all --- the coefficient $W-2\pi r\sin\phi$ should be read as $0$, not negative; we work in the regime $2\pi r\sin\phi<W$ below.)

% \subsection{Rank-one structure and closed-form solution}\label{sec:rank1}
% The right side of both equations in Eq.~\eqref{eq:ode_final} is the same scalar, $\kappa_{uu}\sigma_{uu}-2\kappa_{uv}\sigma_{uv}$, multiplying $\sin(2\phi)$ in the first equation and $\cos(2\phi)$ in the second --- so the $2\times2$ coefficient matrix is rank one, with $(\sin2\phi,\cos2\phi)$ its only nonzero-eigenvalue eigenvector. Writing $\sigma_{uu}(u)=A(u)\sin(2\phi)$, $\sigma_{uv}(u)=A(u)\cos(2\phi)$ (the general solution also admits a null-space term along $(2\sin\phi,\cos\phi)$, which we set to zero, as before, for the minimal, best-conditioned solution carrying a single free constant), the bracket becomes
% $$\kappa_{uu}\sigma_{uu}-2\kappa_{uv}\sigma_{uv} = A(u)\big[\kappa_{uu}\sin2\phi-2\kappa_{uv}\cos2\phi\big] = A(u)\,\frac{2\sin^3\phi\cos\phi}{r}$$
% (using $\kappa_{uu}=\frac{\cos^2\phi}{r},\ \kappa_{uv}=\frac{\sin\phi\cos\phi}{r}$ from Eq.~\ref{eq:curvatures} and standard double-angle identities), and both equations of Eq.~\eqref{eq:ode_final} collapse to the single scalar equation
% \begin{equation}\label{eq:scalar_ode}
%     \boxed{\frac{\mathrm dA}{\mathrm du} = \frac{2\mu(W-2\pi r\sin\phi)\sin^3\phi\cos\phi}{rW}\,A(u).}
% \end{equation}
% Since $r,\phi$ are frozen at fixed $x$, the coefficient on the right is a constant in $u$, and Eq.~\eqref{eq:scalar_ode} integrates immediately to
% \begin{equation}\label{eq:A_closed}
%     \boxed{A(u) = A_0\exp\left[\frac{2\mu(W-2\pi r\sin\phi)\sin^3\phi\cos\phi}{rW}\,u\right],}
% \end{equation}
% with $A_0:=A(0)$ the single free constant carrying the physical scale of the whole solution (the holding tension at the fixed, base end), so that
% \begin{equation}\label{eq:sigma_closed}
%     \boxed{\begin{aligned}
%     \sigma_{uu}(u) &= A_0\sin(2\phi)\exp\left[\frac{2\mu(W-2\pi r\sin\phi)\sin^3\phi\cos\phi}{rW}\,u\right], \\
%     \sigma_{uv}(u) &= A_0\cos(2\phi)\exp\left[\frac{2\mu(W-2\pi r\sin\phi)\sin^3\phi\cos\phi}{rW}\,u\right].
%     \end{aligned}}
% \end{equation}

\subsection{Sign and monotonicity of the contact pressure}\label{sec:sign_monotonicity}
% Section~\ref{sec:slip_direction} assumed $\sigma_{rr}\le0$ (compression) throughout, in order to write the Coulomb friction magnitude as $\mu$ times $-\sigma_{rr}$; we now verify this self-consistently. Take $A_0>0$ (a positive base tension). Since $\phi\in(0^\circ,90^\circ)$, $\sin\phi,\cos\phi>0$, so every prefactor appearing above is non-negative, and $\sigma_{uu}(u)=A_0\sin(2\phi)e^{(\cdots)u}\ge0$ for all $u\in[0,L]$: the axial stress is tensile everywhere, as it must be for a ribbon that is only ever pulled, never pushed. Substituting Eq.~\eqref{eq:sigma_closed} into the bracket of Eq.~\eqref{eq:pressure_closed},
% \begin{equation}
%     \begin{aligned}
%     \kappa_{uu}\sigma_{uu}(u')-2\kappa_{uv}\sigma_{uv}(u') &= A_0\,\frac{2\sin^3\phi\cos\phi}{r}\exp\left[\frac{2\mu(W-2\pi r\sin\phi)\sin^3\phi\cos\phi}{rW}\,u'\right] \\
%     &\ge 0 \quad\text{for all }u'\in[0,L],
%     \end{aligned}
% \end{equation}
% so the integrand in Eq.~\eqref{eq:pressure_closed} is non-negative throughout, its integral over $u'\in[u,L]$ is non-negative, and the leading minus sign in Eq.~\eqref{eq:pressure_closed} gives
% \begin{equation}\label{eq:sigma_rr_nonpositive}
%     \sigma_{rr}\left(u,v,\frac{t}{2}\right) \le 0 \quad\text{for all }(u,v),
% \end{equation}
% confirming the compression assumed throughout Section~\ref{sec:friction}. The same holds for $\sigma_{rr}\big(u,v,-\frac{t}{2}\big)$, since
% \begin{equation}
%     \sigma_{rr}\left(u,v,-\frac{t}{2}\right)=\sigma_{rr}\left(u,v,\frac{t}{2}\right)-t\Big[\kappa_{uu}\sigma_{uu}-2\kappa_{uv}\sigma_{uv}\Big](u)
% \end{equation}
% (Eq.~\ref{eq:pressure_jump}) subtracts a term that is itself non-negative. Beyond the sign, Eq.~\eqref{eq:pressure_closed} is manifestly \emph{monotonic}: as $u$ decreases from $L$ (where it is exactly zero, by the base case) toward $0$, the integration range $[u,L]$ only grows, so $\left|\sigma_{rr}\left(u,v,\frac{t}{2}\right)\right|$ increases monotonically. This matches the physical picture directly: a layer near the base end ($u=0$) has every other wound layer stacked outside it and so carries the full accumulated contact pressure, while a layer at the active end ($u=L$) has nothing wound outside it and carries none.

% \subsection{Force-displacement relation}\label{sec:force_displacement}
% The resultant force at the active end, projected onto the global pulling direction, is $F(x)=W\big[\sigma_{uu}(L)\sin\phi+\sigma_{uv}(L)\cos\phi\big]$ (Section~\ref{sec:closed_form}, opening). Substituting Eq.~\eqref{eq:sigma_closed} at $u=L$ and using $\sin(2\phi)\sin\phi+\cos(2\phi)\cos\phi=\cos(2\phi-\phi)=\cos\phi$ to collapse the double-angle dependence,
% \begin{equation}
%     F(x) = WA_0\cos\phi\,\exp\left[\frac{2\mu(W-2\pi r\sin\phi)\sin^3\phi\cos\phi}{rW}\,L\right].
% \end{equation}
% Writing this out in terms of $x$ via $\sin\phi=\frac{x}{L}$, $\cos\phi=\frac{\sqrt{L^2-x^2}}{L}$, $r=\frac{\sqrt{L^2-x^2}}{\theta_L}$ (Eq.~\ref{eq:phi}--\ref{eq:r}), the exponent simplifies (the two factors of $\sqrt{L^2-x^2}$, one from $\cos\phi$ and one from $\frac{1}{r}$, cancel) to
% \begin{equation}
%     \frac{2\mu(W-2\pi r\sin\phi)\sin^3\phi\cos\phi}{rW}\,L = \mu\left(1-\frac{2\pi x\sqrt{L^2-x^2}}{\theta_L LW}\right)\frac{2\theta_L x^3}{L^3},
% \end{equation}
% giving the closed-form force---displacement law
% \begin{equation}\label{eq:F_closed}
%     \boxed{F(x) = \frac{W A_0\sqrt{L^2-x^2}}{L}\,\exp\left[\mu\left(1-\frac{2\pi x\sqrt{L^2-x^2}}{\theta_L LW}\right)\frac{2\theta_L x^3}{L^3}\right],}
% \end{equation}
% valid for $2\pi x\sqrt{L^2-x^2}<\theta_L LW$ (Section~\ref{sec:width_reduction}), i.e., before the overlap band consumes the full width $W$. As in the leading-order picture of Section~\ref{sec:pressure_recursion}, $F(0)=\frac{WA_0}{L}\cdot L=WA_0$ is set entirely by the base tension $A_0$, and the exponential growth in $x$ is exactly the accumulated, capstan-type effect of Section~\ref{sec:accumulated_pressure}: each additional bit of wound-over contact multiplies the required pulling force rather than adding to it.

%TODO: this closed form drops two effects flagged along the way as secondary but not rigorously shown to be small: the curvature-correction term of Eq.~\eqref{eq:curvature_correction} (needs the nonlocal, accumulated $\sigma_{rr}(\frac{t}{2})+\sigma_{rr}(-\frac{t}{2})$, dropped in Eq.~\ref{eq:fu_width_integrated}) and the shear-divergence term of Eq.~\eqref{eq:outofplane_integrated}. Also unaddressed: the width-edge truncation of Eq.~\eqref{eq:pressure_closed}, and, if a smooth (rather than hard) turn-on of the overlap band at $v=-\frac{W}{2}+2\pi r\sin\phi$ is later needed for numerical purposes, replacing that hard cutoff with a sigmoidal gate --- unnecessary for the closed form above, since the hard cutoff already integrates over $v$ exactly.


\section{Experimental validation}
\begin{figure}[p]
    \centering
    \includegraphics[width=\textwidth]{figs/instron_plot.png}
    \caption{Instron force-displacement data, with raw quantized data (grey), smoothed data (solid colored), and curve fit (dashed colored).}\label{fig:yoyo}
\end{figure}

\end{document}
